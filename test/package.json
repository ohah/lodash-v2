{
  "name": "@lodash-v2/test",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "동등성 테스트·벤치마크 — 배포 환경과 동일하게 @lodash-v2/core를 의존성으로 사용",
  "scripts": {
    "test": "bun test src/__tests__",
    "run": "bun run src/run.ts",
    "compare": "bun run src/compare.ts",
    "generate-feedback": "bun run scripts/generate-feedback-report.ts"
  },
  "dependencies": {
    "@lodash-v2/core": "workspace:*",
    "es-toolkit": "1.44",
    "lodash": "^4.17.23"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
